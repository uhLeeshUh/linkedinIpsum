#import "../fragments/full-bio-chunk.graphql"

mutation bioCreate($industryId: ID!, $name: String!) {
  bioCreate(input: { industryId: $industryId, name: $name }) {
    id
    name
    bioChunks {
      ...FullBioChunk
    }
  }
}
